# Tasks

## 1. 错误 → 专题映射

- [x] 1.1 建立 `data/errorDrills.ts` 静态映射（大臂抢 / 反手抬臂 / 被球顶住 / 脚到不稳 → 专题 + Primary Cue），验证四种问题均能返回推荐
- [x] 1.2 接入训练生成器，验证「选问题 → 推荐专题 → 生成训练 + 建议 Cue」流程

## 2. 整合训练

- [x] 2.1 实现 `useIntegration` 来球状态机（出球 → 判断 → 移动 → 稳定 → 发力 → 击球 → 还原），验证全链路按序执行
- [x] 2.2 实现 Power Chain Level 4–7（随机触发 / 方向 / 正反手 / 球路），验证各等级随机维度正确
- [x] 2.3 实现 Full Shadow Rally 连续回合，验证随机来球连续、击球后提示还原

## 3. 验收

- [x] 3.1 端到端走通「选问题 → 生成推荐训练 → 整合训练全链路」，验证 `pnpm verify` 全绿且关键流程经浏览器真实验证
