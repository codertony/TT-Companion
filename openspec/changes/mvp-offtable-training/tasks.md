# Tasks

## 1. 依赖与路由骨架

- [x] 1.1 安装 `zustand`、`react-router-dom` 依赖，验证 `pnpm install` 成功且 `pnpm verify` 全绿
- [x] 1.2 接入 `HashRouter` 与底部 4 Tab（首页 / 训练 / 数据 / 我的），验证 4 个 Tab 可切换、桌面 480px 单列居中
- [x] 1.3 建立 `stores/`（cue / settings / session / feedback / train）骨架并接入 Zustand persist，验证 `pnpm typecheck` 通过

## 2. 场景与时长选择

- [x] 2.1 实现场景选择页（5 场景 + 地铁「坐姿/站姿」二级选择），验证选「地铁」时出现坐/站二级选择
- [x] 2.2 实现时长选择页（1/3/5/10 分钟，3 与 5 标「推荐」），验证四个档位正确渲染且可选中

## 3. 动作库与训练生成器

- [x] 3.1 建立 `data/exercises.ts`（约 24 个动作，含 timedPrompts），验证每个动作字段齐全、反应类动作 timedPrompts 为空
- [x] 3.2 实现 `lib/generator.ts` 纯函数，验证单测覆盖场景过滤、Cue 命中置顶、时长填充、纯函数（相同输入相同输出）
- [x] 3.3 实现 `lib/cue.ts` 生命周期纯函数（激活 / 归档 / 完成判定），验证单测覆盖「唯一 active」与「连续 2 周改善自动 done」

## 4. 训练执行与时机化提示

- [x] 4.1 实现 `useCountdown`（用 endAt 时间戳校正漂移），验证倒计时到点准确触发 onDone
- [x] 4.2 实现 `useTimedPrompts` + `Stepper`，验证按 `atSec` 到点切换提示、任意时刻仅 1 条、顶部 ONE CUE 常驻
- [x] 4.3 训练完成写入 session 记录，验证刷新页面后该会话记录仍存在

## 5. 反应训练与音频提示

- [x] 5.1 实现 `useReaction` 状态机与反应训练页（5 种刺激模式 + 可配置参数 + 「正确/跳过」自评），验证 5 种模式各可跑一轮
- [x] 5.2 实现 `useAudioCue` 与语音训练页，验证词库随机播报、间隔在配置范围随机、可随时停止

## 6. ONE CUE 与周末反馈

- [x] 6.1 实现 ONE CUE 管理页（新建 / Backlog 排序 / 激活 / 历史），验证任意时刻最多 1 个 active、激活新 Cue 旧 Cue 自动归档
- [x] 6.2 实现周末反馈页（4 档 + 备注 + 完成判定），验证「连续 2 周改善」自动将 Cue 标记为 done

## 7. 数据统计与持久化

- [x] 7.1 实现数据页（周完成率 / 连续天数 / 反应趋势折线），验证折线趋势正确渲染、无雷达图
- [x] 7.2 实现数据导出 / 清空，验证导出为完整 JSON、清空需二次确认

## 8. 集成验收

- [x] 8.1 端到端走通「选场景 → 选时长 → 生成 → 执行 → 完成 → 周末反馈」，验证 `pnpm verify` 全绿且关键用户流程经浏览器真实验证
